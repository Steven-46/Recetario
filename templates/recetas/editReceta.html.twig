{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} - Editar receta
{% endblock %}

{% block body %}
    {{ parent() }}
    {{ dump(receta) }}
    <div class="row h-100 justify-content-center align-content-center">
        <div class="col-6 bg-white rounded my-3 p-5">
            <h1>Editar</h1>
            {{ form(formulario) }}
        </div>
    </div>
    </div>
{% endblock %}