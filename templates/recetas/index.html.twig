{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} - Registrar recetas
{% endblock %}

{% block body %}
    {{ parent() }}
    <div class="container h-100 justify-content-center ">
            <div class="row justify-content-center ">
                <div class="col-8 bg-white rounded my-3 p-5">
                    <h1>Registro</h1>
                    {{ form(formulario) }}
                </div>
            </div>
    </div>


{% endblock %}
